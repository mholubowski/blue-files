<!DOCTYPE html>
<html>
<head>
  <title>BlueFiles</title>
  <%= stylesheet_link_tag    "application", :media => "all" %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>
</head>
<body>
<%= render 'layouts/header' %>
<div class="container">
	<% flash.each do |key, value| %>
			<div id="flash-fade" class="alert alert-<%= key %>"><%= value %></div>
	<% end %>
<h1>current: <%= current_account.username if signed_in? %></h1>	
<%= yield %>
</div>
<%= render 'layouts/footer' %>
￼<%= debug(params) if Rails.env.development? %>

</body>
</html>
